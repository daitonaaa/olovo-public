import styled from 'styled-components';

export const Wrapper = styled.div`
  flex: 1;
  align-items: center;
  display: flex;
  justify-content: flex-end;
  color: #fff;

  a {
    color: #fff;
  }
`;
