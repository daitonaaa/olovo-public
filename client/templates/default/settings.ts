import { TemplateSettings } from '../../core/shared/types'

import { PageDefaultLayout } from './components/layout'

const settings: TemplateSettings = {
  defaultLayout: PageDefaultLayout,
}

export default settings
